bind [:ctrl;cmd ${leftTwoThird}
bind ]:ctrl;cmd ${rightTwoThird}

bind [:ctrl,shift;cmd ${leftThird}
bind ]:ctrl,shift;cmd ${rightThird}

bind ;:ctrl,shift;cmd ${middleThird}

bind ,:ctrl,cmd ${full}

bind g:ctrl,cmd ${leftHalf}
bind g:ctrl,shift,cmd ${leftFourth}
bind ':ctrl,cmd ${rightHalf}
bind ':ctrl,shift,cmd ${rightFourth}

bind ;:ctrl,cmd move screenOriginX+((screenSizeX-windowSizeX)/2);screenOriginY+((screenSizeY-windowSizeY)/2) windowSizeX;windowSizeY
