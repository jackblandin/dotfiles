#! /bin/bash -e

morning () {
  pgstart
  cd /Users/jackblandin/tc/service_manager
  git pull
  vagrant up
  bundle exec rake start:update
  if [ "$1" = "off" ]; then
    bundle exec rake stop
  fi
}

morning
